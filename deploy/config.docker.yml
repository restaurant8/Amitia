server:
  port: 8899
  host: "0.0.0.0"
  mode: "release"

storage:
  dataDir: "data"

jwt:
  secret: ""
  expireDays: 7

app:
  name: "Amitia"
  version: "1.0.0"
  deployMode: "server-docker"

chat:
  mergeWindowMs: 6000
  contextWindowMaxRounds: 20

qdrant:
  host: "qdrant"
  port: 6333
  collectionName: "memory_embeddings"
  vectorDim: 1536
  limit: 10
  collections:
    memory_embeddings:
      name: "memory_embeddings"
      vectorDim: 1536
    working_memory:
      name: "working_memory"
      vectorDim: 1536
    user_profiles:
      name: "user_profiles"
      vectorDim: 1536
    episodic_memories:
      name: "episodic_memories"
      vectorDim: 1536

embedding:
  modelName: "text-embedding-3-small"
  baseUrl: ""
  apiKey: ""

surrealdb:
  host: "surrealdb"
  port: 8000
  namespace: "uai"
  database: "memory_graph"
  username: "root"
  password: "root"
  dataPath: "data/graph.db"
